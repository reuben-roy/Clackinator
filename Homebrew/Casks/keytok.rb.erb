cask "keytok" do
  version "<%= version %>"
  sha256 "<%= sha256 %>"

  url "https://github.com/<%= owner %>/<%= repo %>/releases/download/v#{version}/KeyTokDirect-#{version}.zip"
  name "KeyTok"
  desc "Mechanical keyboard sound utility for macOS"
  homepage "https://github.com/<%= owner %>/<%= repo %>"

  app "KeyTokDirect.app"
end
